<nav class="navbar navbar-dark bg-primary">
  <div class="container-fluid">
    <%= link_to "Geocoding", root_path, class: "navbar-brand me-auto" %>

    <% if Current.session.present? %>
      <%= button_to "Sign out", session_path,
                    method: :delete,
                    class: "btn btn-outline-light" %>
    <% else %>
      <%= link_to "Log in", new_session_path, class: "btn text-white me-2" %>
      <%= link_to "Sign up", new_registration_path,
                  class: "btn btn-outline-light" %>
    <% end %>
  </div>
</nav>
